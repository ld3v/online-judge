export enum Role {
  Admin = 'admin',
  User = 'user',
}

export enum Action {
  Active = 'active',
  Lock = 'lock',
  Unlock = 'unlock',
}

export enum LoginMethod {
  Local = 'local',
  Google = 'google',
}
